[
  {
    "name": "/settings rejects non-admin users",
    "steps": [
      {
        "send": { "text": "/settings" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "This command is for admins only."
            }
          }
        ]
      }
    ]
  }
]
